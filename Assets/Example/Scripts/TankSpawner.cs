using SheetCodes;
using UnityEngine;

public class TankSpawner : MonoBehaviour
{
    //public void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.A))
    //        Instantiate(TankIdentifier.Tank1.GetRecord().Prefab);
    //    if (Input.GetKeyDown(KeyCode.B))
    //        Instantiate(TankIdentifier.Tank2.GetRecord().Prefab);
    //    if (Input.GetKeyDown(KeyCode.C))
    //        Instantiate(TankIdentifier.Tank3.GetRecord().Prefab);
    //}
}
